/*

Write a constructor function that produces objects
matching the description in spec/flower_spec.js.

- flowers have two attributes, name and color
- write tests that demonstrate that our constructor func produces objs with correct names and colors

*/

var Flower = function(name, color) {
  this.name = name;
  this.color = color;
}
